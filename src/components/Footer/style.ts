import styled from 'styled-components';

export const FooterContainer = styled.footer`
  background: #eeeeee;
  padding: 0.625rem;

  width: 100%;

  position: relative;
  bottom: 0;

  p {
    color: var(--black-900);
    font-weight: 400;
    font-size: 0.75rem;
    width: 100%;
    text-align: center;
  }
`;
